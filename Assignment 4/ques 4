#include <iostream>
using namespace std;
#include<queue>

int main()
{
    queue<char>q;
    int freq[256] ={0};
    
    cout<<"enter characters";
    
     char ch;
    while (cin >> ch) {
        freq[(int)ch]++;

        if (freq[(int)ch] == 1) {
            q.push(ch);
        }

        while (!q.empty() && freq[(int)q.front()] > 1) {
            q.pop();
        }

        if (q.empty()) {
            cout << "-1 ";
        } else {
            cout << q.front() << " ";
        }
    }
    cout << endl;


    return 0;
}
